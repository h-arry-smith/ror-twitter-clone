<div class="card">
    <div class="card-body d-flex justify-content-between">
        <div>
            <h5><%= user.display_name %></h5>
            <div class="text-secondary fs-6"><%= truncate(user.bio, length: 80, omission: '...') %></div>
        </div>
        <%= link_to profile_path(user) do %>
            <h6>@<%= user.handle %></h6>
        <% end %>
    </div>
</div>
